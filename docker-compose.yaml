version: '4'
services:
  frontend:
    build:
      context: client
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    stdin_open: true
  backend:
    build:
      context: backend
      dockerfile: Dockerfile
    ports:
      - 5000:5000
